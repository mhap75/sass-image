import { IParams } from "@/types/types";

const AddTransformationType: React.FC<
  IParams<{ transformationType: string }>
> = ({ params: { transformationType } }) => {
  return <div>AddTransformationType</div>;
};

export default AddTransformationType;
