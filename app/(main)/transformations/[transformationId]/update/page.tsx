import { IParams } from "@/types/types";

const UpdateTransformationId: React.FC<
  IParams<{ transformationId: string }>
> = ({ params: { transformationId } }) => {
  return <div>UpdateTransformationId</div>;
};

export default UpdateTransformationId;
